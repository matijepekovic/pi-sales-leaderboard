name: v68 write probe
permissions:
  contents: write
on:
  push:
    branches: [build/v68-pinch-cap]
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo write-probe-ok
