/* Software Settings owns version identity and signed Windows updates. */
(function(){
  const runtime=window.StatsSettings,$=id=>document.getElementById(id);let remoteVersion="";const status=text=>{if($("softwareUpdateStatus"))$("softwareUpdateStatus").textContent=text||"";};
  async function loadVersion(){try{const data=await runtime.api("/api/system/version");$("softwareVersionCurrent").textContent=`Software version: ${String(data.version||"unknown")}`;}catch(_){$("softwareVersionCurrent").textContent="Software version: unavailable";}}
  async function check(){const check=$("softwareCheckUpdate"),install=$("softwareInstallUpdate");check.disabled=true;install.disabled=true;remoteVersion="";status("Checking for updates…");try{const data=await runtime.api("/api/windows/update/check",{method:"POST"});if(data.available){remoteVersion=String(data.latest||"");status(remoteVersion?`Stats ${remoteVersion} is available.`:"Update available.");install.disabled=false;}else status("Stats is up to date.");}catch(error){status(error.message);}finally{check.disabled=false;}}
  async function waitForRestart(){status("Installing update…");let sawOffline=false;for(let i=0;i<100;i++){await new Promise(resolve=>setTimeout(resolve,1200));try{const response=await fetch(`/health?ts=${Date.now()}`,{cache:"no-store"});if(response.ok&&(sawOffline||i>8)){location.reload();return true;}}catch(_){sawOffline=true;}}return false;}
  async function install(){if(!remoteVersion)return;const check=$("softwareCheckUpdate"),button=$("softwareInstallUpdate");check.disabled=true;button.disabled=true;status(`Downloading Stats ${remoteVersion}…`);try{const data=await runtime.api("/api/windows/update/install",{method:"POST"});if(data.installed||data.installing){if(await waitForRestart())return;status("Install started, but Stats did not return yet.");}else{remoteVersion="";status("Stats is up to date.");}}catch(error){status(error.message);}finally{check.disabled=false;button.disabled=!remoteVersion;}}
  function bind(){$("softwareCheckUpdate")?.addEventListener("click",check);$("softwareInstallUpdate")?.addEventListener("click",install);loadVersion();}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",bind,{once:true});else bind();
})();
