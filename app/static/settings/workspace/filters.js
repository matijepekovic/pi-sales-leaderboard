import {esc,request,json,table} from "./runtime.js";

export function createFiltersWorkspace({host,notify,onChanged,getReports}){
  const state={filters:[],reports:[],editor:null,inspections:{},focus:0,preview:null};
  let inlineCallback=null;
  const report=id=>state.reports.find(item=>String(item.id)===String(id))||null;
  const filter=id=>state.filters.find(item=>String(item.id)===String(id))||null;

  async function load(){
    const [f,r]=await Promise.all([request("/api/filters"),request("/api/data/reports")]);
    state.filters=f.filters||[];state.reports=r.reports||[];render();onChanged?.();
  }
  async function inspect(reportId,force=false){if(!reportId)return null;if(force)delete state.inspections[reportId];if(!state.inspections[reportId])state.inspections[reportId]=await request(`/api/data/reports/${encodeURIComponent(reportId)}/inspect`);return state.inspections[reportId];}

  function list(){return `<div class="card"><div class="toolbar"><div><h2>Display Filters</h2><div class="muted small">Reusable filters over data Stats already pulled.</div></div><button class="button primary" data-action="new">+ Filter</button></div><div class="stack" style="margin-top:12px">${state.filters.map(item=>`<div class="item"><div class="toolbar"><div><strong>${esc(item.name)}</strong><div class="meta">${(item.rules||[]).length} rule${(item.rules||[]).length===1?"":"s"}</div></div><div class="actions"><button class="button" data-action="edit" data-id="${esc(item.id)}">Edit / Test</button><button class="button danger" data-action="delete" data-id="${esc(item.id)}">Delete</button></div></div>${(item.rules||[]).map(rule=>`<div class="meta">${esc(report(rule.report_id)?.name||rule.report_id)} · ${esc(rule.field)} · ${esc(rule.operator)} · ${esc(rule.value)}</div>`).join("")}</div>`).join("")||'<div class="empty">No Display Filters yet.</div>'}</div></div>`;}

  function rule(rule,index){const info=state.inspections[rule.report_id],fields=info?.fields||[],field=fields.find(item=>String(item.key)===String(rule.field));return `<div class="rule" data-rule="${index}"><div class="grid3"><div><label>Report</label><select data-rule-report="${index}"><option value="">Choose Report…</option>${state.reports.map(item=>`<option value="${esc(item.id)}" ${item.id===rule.report_id?"selected":""}>${esc(item.name)}</option>`).join("")}</select></div><div><label>Field</label><select data-rule-field="${index}"><option value="">Choose field…</option>${fields.map(item=>`<option value="${esc(item.key)}" ${item.key===rule.field?"selected":""}>${esc(item.label||item.key)}</option>`).join("")}</select></div><div><label>Match</label><select data-rule-operator="${index}"><option value="equals" ${rule.operator==="equals"?"selected":""}>Equals</option><option value="not_equals" ${rule.operator==="not_equals"?"selected":""}>Does not equal</option><option value="contains" ${rule.operator==="contains"?"selected":""}>Contains</option><option value="not_contains" ${rule.operator==="not_contains"?"selected":""}>Does not contain</option></select></div></div><div class="grid" style="margin-top:9px"><div><label>Value</label><input data-rule-value="${index}" list="filter-values-${index}" value="${esc(rule.value||"")}" placeholder="Choose or type"><datalist id="filter-values-${index}">${(field?.sample_values||[]).map(value=>`<option value="${esc(value)}"></option>`).join("")}</datalist></div><div class="actions" style="align-items:end"><button class="button danger" data-action="remove-rule" data-index="${index}">Remove Rule</button></div></div></div>`;}

  function actualData(){const current=state.editor?.rules?.[Math.min(state.focus,state.editor.rules.length-1)]||{},info=state.inspections[current.report_id];if(!current.report_id)return '<div class="empty">Choose a Report in a rule. Its pulled rows and values will appear here.</div>';if(!info)return '<div class="empty">Loading pulled data…</div>';return `<div class="card"><div class="toolbar"><div><h3>${esc(info.report_name)}</h3><div class="meta">${Number(info.total_rows||0)} pulled rows</div></div><button class="button" data-action="reload-data" data-report-id="${esc(current.report_id)}">Reload Data</button></div>${table(info.fields||[],info.sample_rows||[],20)}<div class="stack" style="margin-top:10px">${(info.fields||[]).map(field=>`<div class="item"><strong>${esc(field.label||field.key)}</strong><div class="meta">${esc(field.key)}</div><div class="field-values">${(field.sample_values||[]).slice(0,100).map(value=>`<span>${esc(value)}</span>`).join("")||'<span>No values</span>'}</div></div>`).join("")}</div></div>`;}

  function tested(){if(!state.preview)return"";return `<div class="card"><h3>Filter Test</h3><div class="muted small">Same Filter engine used by Screens.</div><div class="stack" style="margin-top:10px">${(state.preview.reports||[]).map(item=>`<div class="section-preview"><strong>${esc(item.report_name)}</strong><div class="meta">${Number(item.matched_rows||0)} of ${Number(item.total_rows||0)} rows match</div>${table(item.fields||[],item.rows||[],20)}</div>`).join("")}</div></div>`;}

  function editor(){if(!state.editor)return"";return `<div class="editor"><div class="toolbar"><div><h2>${state.editor.id?"Edit":"Create"} Filter</h2><div class="muted small">Look at the actual pulled data while creating the rules.</div></div><button class="button" data-action="close">Close</button></div><div class="split" style="margin-top:12px"><div><label>Filter name</label><input data-filter-name value="${esc(state.editor.name||"")}" placeholder="Example: Olympia"><div class="toolbar" style="margin-top:12px"><h3>Rules</h3><button class="button" data-action="add-rule">+ Rule</button></div><div class="stack">${(state.editor.rules||[]).map(rule).join("")||'<div class="empty">Add a rule.</div>'}</div><div class="actions" style="margin-top:12px"><button class="button" data-action="test">Test Filter</button><button class="button primary" data-action="save">Save Filter</button></div>${tested()}</div><div>${actualData()}</div></div></div>`;}
  function render(){host.innerHTML=list()+editor();bind();}

  function sync(){if(!state.editor)return;const name=host.querySelector('[data-filter-name]');if(name)state.editor.name=name.value;host.querySelectorAll('[data-rule-report]').forEach(el=>{const i=Number(el.dataset.ruleReport);if(state.editor.rules[i])state.editor.rules[i].report_id=el.value});host.querySelectorAll('[data-rule-field]').forEach(el=>{const i=Number(el.dataset.ruleField);if(state.editor.rules[i])state.editor.rules[i].field=el.value});host.querySelectorAll('[data-rule-operator]').forEach(el=>{const i=Number(el.dataset.ruleOperator);if(state.editor.rules[i])state.editor.rules[i].operator=el.value});host.querySelectorAll('[data-rule-value]').forEach(el=>{const i=Number(el.dataset.ruleValue);if(state.editor.rules[i])state.editor.rules[i].value=el.value});}
  async function open(item=null,callback=null){inlineCallback=callback;state.editor=item?structuredClone(item):{name:"",rules:[{report_id:state.reports[0]?.id||"",field:"",operator:"equals",value:""}]};state.focus=0;state.preview=null;for(const r of state.editor.rules||[])if(r.report_id)try{await inspect(r.report_id);}catch(error){notify(error.message,true)}render();}
  async function save(){sync();notify("Saving Filter…");try{const endpoint=state.editor.id?`/api/filters/${encodeURIComponent(state.editor.id)}`:"/api/filters";const data=await request(endpoint,json(state.editor.id?"PUT":"POST",state.editor));const saved=data.filter,cb=inlineCallback;state.editor=null;state.preview=null;inlineCallback=null;notify("Filter saved.");await load();if(cb)cb(saved);}catch(error){notify(error.message,true)}}
  async function test(){sync();notify("Testing Filter…");try{state.preview=await request("/api/filters/preview",json("POST",state.editor));notify("Filter test complete.");}catch(error){state.preview=null;notify(error.message,true)}render();}

  function bind(){host.querySelectorAll('[data-action]').forEach(button=>button.addEventListener("click",async()=>{const a=button.dataset.action,id=button.dataset.id,index=Number(button.dataset.index||0);sync();if(a==="new")return open();if(a==="edit")return open(filter(id));if(a==="close"){state.editor=null;state.preview=null;inlineCallback=null;render();return}if(a==="add-rule"){state.editor.rules.push({report_id:state.reports[0]?.id||"",field:"",operator:"equals",value:""});state.focus=state.editor.rules.length-1;if(state.editor.rules[state.focus].report_id)try{await inspect(state.editor.rules[state.focus].report_id)}catch(error){notify(error.message,true)}render();return}if(a==="remove-rule"){state.editor.rules.splice(index,1);state.focus=Math.max(0,Math.min(state.focus,state.editor.rules.length-1));render();return}if(a==="reload-data"){try{await inspect(button.dataset.reportId,true);render()}catch(error){notify(error.message,true)}return}if(a==="test")return test();if(a==="save")return save();if(a==="delete"){if(!confirm("Delete this Filter?"))return;try{await request(`/api/filters/${encodeURIComponent(id)}`,{method:"DELETE"});notify("Filter deleted.");await load()}catch(error){notify(error.message,true)}}}));host.querySelectorAll('[data-rule]').forEach(el=>el.addEventListener("click",()=>{state.focus=Number(el.dataset.rule)}));host.querySelectorAll('[data-rule-report]').forEach(el=>el.addEventListener("change",async()=>{sync();const i=Number(el.dataset.ruleReport);state.editor.rules[i].report_id=el.value;state.editor.rules[i].field="";state.editor.rules[i].value="";state.focus=i;if(el.value)try{await inspect(el.value)}catch(error){notify(error.message,true)}render()}));host.querySelectorAll('[data-rule-field]').forEach(el=>el.addEventListener("change",()=>{sync();state.focus=Number(el.dataset.ruleField);render()}));}

  return {load,openCreate:callback=>open(null,callback),list:()=>state.filters.slice()};
}
