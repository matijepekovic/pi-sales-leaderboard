import {esc,request,json,table,debounce} from "./runtime.js";

export function createScreensWorkspace({host,notify,onChanged,filterManager}){
  const state={screens:[],reports:[],filters:[],editor:null,step:0,preview:null,theme:null};
  const report=id=>state.reports.find(item=>String(item.id)===String(id))||null;
  const filter=id=>state.filters.find(item=>String(item.id)===String(id))||null;
  const screen=id=>state.screens.find(item=>String(item.id)===String(id))||null;

  async function load(){const [s,r,f]=await Promise.all([request("/api/screens"),request("/api/data/reports"),request("/api/filters")]);state.screens=s.screens||[];state.reports=r.reports||[];state.filters=f.filters||[];render();onChanged?.();}
  function blank(){return {name:"New Screen",reports:[],filter_ids:[],tables:[],theme_mode:"inherited"};}
  function syncTables(item){const selected=new Set(item.reports||[]);item.tables=(item.tables||[]).filter(t=>selected.has(t.report_id));for(const reportId of item.reports||[]){if(item.tables.some(t=>t.report_id===reportId))continue;const fields=report(reportId)?.fields||[];item.tables.push({report_id:reportId,columns:fields.slice(0,8).map(f=>f.key),sort_field:"",sort_direction:"desc",limit:100});}}

  function list(){return `<div class="card"><div class="toolbar"><div><h2>Screens</h2><div class="muted small">Compose pulled Reports, reusable Filters, ranking and visual treatment.</div></div><button class="button primary" data-action="new">+ Screen</button></div><div class="stack" style="margin-top:12px">${state.screens.map(item=>`<div class="item"><div class="toolbar"><div><strong>${esc(item.name)}</strong><div class="meta">${(item.reports||[]).length} Reports · ${(item.filter_ids||[]).length} Filters · ${esc(item.theme_mode||"inherited")} theme</div></div><div class="actions"><button class="button" data-action="edit" data-id="${esc(item.id)}">Edit</button><button class="button" data-action="open-display" data-id="${esc(item.id)}">Open</button><button class="button danger" data-action="delete" data-id="${esc(item.id)}">Delete</button></div></div></div>`).join("")||'<div class="empty">No Screens yet. Create one from your pulled Reports.</div>'}</div></div>`;}
  const steps=()=>`<div class="stepbar">${["1. Data","2. Filters","3. Display","4. Theme"].map((name,index)=>`<button class="step ${state.step===index?"active":""}" data-step="${index}" type="button">${name}</button>`).join("")}</div>`;
  function dataStep(item){return `<div><h3>Choose Data</h3><div class="muted small">Choose one or more pulled Reports.</div><div class="check-list" style="margin-top:10px">${state.reports.map(r=>`<label class="check-row"><input type="checkbox" data-screen-report="${esc(r.id)}" ${(item.reports||[]).includes(r.id)?"checked":""}><span><strong>${esc(r.name)}</strong><br><span class="muted small">${(r.fields||[]).length} fields · ${esc(r.status||"Not pulled yet")}</span></span></label>`).join("")||'<div class="empty">Create and pull a Report in Data first.</div>'}</div></div>`;}
  function filterStep(item){return `<div><div class="toolbar"><div><h3>Assign Filters</h3><div class="muted small">Choose existing Display Filters or create one now.</div></div><button class="button" data-action="new-filter">+ Create Filter</button></div><div class="check-list" style="margin-top:10px">${state.filters.map(f=>`<label class="check-row"><input type="checkbox" data-screen-filter="${esc(f.id)}" ${(item.filter_ids||[]).includes(f.id)?"checked":""}><span><strong>${esc(f.name)}</strong>${(f.rules||[]).map(rule=>`<br><span class="muted small">${esc(report(rule.report_id)?.name||rule.report_id)} · ${esc(rule.field)} ${esc(rule.operator)} ${esc(rule.value)}</span>`).join("")}</span></label>`).join("")||'<div class="empty">No Filters yet. Create one here.</div>'}</div></div>`;}
  function tableBox(t){const r=report(t.report_id),fields=r?.fields||[];return `<div class="report-box"><h4>${esc(r?.name||t.report_id)}</h4><label>Columns</label><div class="check-list" style="grid-template-columns:repeat(auto-fit,minmax(160px,1fr))">${fields.map(f=>`<label class="check-row"><input type="checkbox" data-column-report="${esc(t.report_id)}" value="${esc(f.key)}" ${(t.columns||[]).includes(f.key)?"checked":""}><span>${esc(f.label||f.key)}</span></label>`).join("")}</div><div class="grid3" style="margin-top:10px"><div><label>Rank / sort by</label><select data-sort-report="${esc(t.report_id)}"><option value="">Source order</option>${fields.map(f=>`<option value="${esc(f.key)}" ${t.sort_field===f.key?"selected":""}>${esc(f.label||f.key)}</option>`).join("")}</select></div><div><label>Direction</label><select data-direction-report="${esc(t.report_id)}"><option value="desc" ${t.sort_direction!=="asc"?"selected":""}>Highest first</option><option value="asc" ${t.sort_direction==="asc"?"selected":""}>Lowest first</option></select></div><div><label>Rows</label><input data-limit-report="${esc(t.report_id)}" type="number" min="1" max="500" value="${Number(t.limit||100)}"></div></div></div>`;}
  function displayStep(item){syncTables(item);return `<div><div class="toolbar"><div><h3>Create Display</h3><div class="muted small">Choose visible columns and how people are ranked.</div></div><button class="button" data-action="preview">Refresh Preview</button></div><div class="stack" style="margin-top:10px">${(item.tables||[]).map(tableBox).join("")||'<div class="empty">Choose Reports first.</div>'}</div></div>`;}
  function colorsEditor(){if(!state.editor?.id||!state.theme)return"";const colors=state.theme.colors||{};return `<div class="card"><h3>Custom Theme</h3><div class="grid3">${Object.entries(colors).map(([key,value])=>`<div><label>${esc(key.replaceAll('_',' '))}</label><input data-theme-color="${esc(key)}" type="color" value="${esc(value)}"></div>`).join("")}<div><label>Hero scale</label><input id="screenHeroScale" type="number" min="50" max="200" value="${Number(state.theme.hero_scale||100)}"></div></div><div class="actions" style="margin-top:12px"><button class="button primary" data-action="save-theme">Save Theme</button><button class="button" data-action="reset-theme">Reset Theme</button></div></div><div class="card"><h3>Theme Assets</h3><div class="muted small">Upload visual assets directly into this Screen's Theme scope.</div><div class="grid" style="margin-top:10px">${["background","hero","logo_small","row","champion","medallion","totals_mark"].map(key=>`<div class="item"><strong>${esc(key.replaceAll('_',' '))}</strong>${state.theme.assets?.[key]?`<div class="meta">Applied</div>`:'<div class="meta">Default</div>'}<input data-theme-asset="${key}" type="file" accept="image/png,image/jpeg,image/webp" style="margin-top:8px"><div class="actions" style="margin-top:7px"><button class="button" data-action="upload-asset" data-asset="${key}">Apply</button>${state.theme.assets?.[key]?`<button class="button danger" data-action="reset-asset" data-asset="${key}">Remove</button>`:""}</div></div>`).join("")}</div></div>`;}
  function themeStep(item){return `<div><h3>Theme</h3><div class="muted small">Inherited uses the default Stats presentation. Custom gives this Screen its own colors and assets.</div><div class="check-list" style="margin-top:10px"><label class="check-row"><input type="radio" name="themeMode" data-theme-mode="inherited" ${item.theme_mode!=="custom"?"checked":""}><span><strong>Inherited</strong></span></label><label class="check-row"><input type="radio" name="themeMode" data-theme-mode="custom" ${item.theme_mode==="custom"?"checked":""}><span><strong>Custom</strong></span></label></div>${item.theme_mode==="custom"?(item.id?colorsEditor():'<div class="notice" style="margin-top:12px">Save the Screen once, then its custom Theme can store colors and assets.</div>'):""}</div>`;}

  function preview(){const p=state.preview;if(!p)return '<div class="preview"><strong>Live Preview</strong><div class="muted small" style="margin-top:6px">The preview uses current pulled data and the same server-side Filter engine as Display.</div></div>';if(p.error)return `<div class="preview"><strong>Live Preview</strong><div class="danger-text small">${esc(p.error)}</div></div>`;return `<div class="preview"><strong>${esc(p.screen_name||"Preview")}</strong><div class="meta">${(p.display_filters||[]).map(f=>f.name).join(" · ")||"No Filters"}</div>${(p.sections||[]).map(s=>`<div class="section-preview"><strong>${esc(s.report_name)}</strong><div class="meta">${Number(s.total_rows||0)} matching rows</div>${table(s.fields||[],s.rows||[],20)}</div>`).join("")||'<div class="empty">No data sections.</div>'}</div>`;}
  function editor(){if(!state.editor)return"";const body=[dataStep,filterStep,displayStep,themeStep][state.step](state.editor);return `<div class="editor"><div class="toolbar"><div><h2>${state.editor.id?"Edit":"Create"} Screen</h2><div class="muted small">Data → Filters → Display → Theme.</div></div><button class="button" data-action="close">Close</button></div><div style="margin-top:12px"><label>Screen name</label><input data-screen-name value="${esc(state.editor.name||"")}"></div>${steps()}<div class="split"><div>${body}<div class="toolbar" style="margin-top:14px"><button class="button" data-action="back" ${state.step===0?"disabled":""}>Back</button><div class="actions">${state.step<3?'<button class="button primary" data-action="next">Next</button>':'<button class="button primary" data-action="save">Save Screen</button>'}</div></div></div>${preview()}</div></div>`;}
  function render(){host.innerHTML=list()+editor();bind();}

  function sync(){if(!state.editor)return;const name=host.querySelector('[data-screen-name]');if(name)state.editor.name=name.value;const reports=host.querySelectorAll('[data-screen-report]');if(reports.length){state.editor.reports=Array.from(reports).filter(x=>x.checked).map(x=>x.dataset.screenReport);syncTables(state.editor)}const filters=host.querySelectorAll('[data-screen-filter]');if(filters.length)state.editor.filter_ids=Array.from(filters).filter(x=>x.checked).map(x=>x.dataset.screenFilter);host.querySelectorAll('[data-sort-report]').forEach(el=>{const t=state.editor.tables.find(t=>t.report_id===el.dataset.sortReport);if(t)t.sort_field=el.value});host.querySelectorAll('[data-direction-report]').forEach(el=>{const t=state.editor.tables.find(t=>t.report_id===el.dataset.directionReport);if(t)t.sort_direction=el.value});host.querySelectorAll('[data-limit-report]').forEach(el=>{const t=state.editor.tables.find(t=>t.report_id===el.dataset.limitReport);if(t)t.limit=Number(el.value||100)});const grouped=new Map();host.querySelectorAll('[data-column-report]').forEach(el=>{if(!grouped.has(el.dataset.columnReport))grouped.set(el.dataset.columnReport,[]);if(el.checked)grouped.get(el.dataset.columnReport).push(el.value)});for(const [reportId,columns] of grouped){const t=state.editor.tables.find(t=>t.report_id===reportId);if(t)t.columns=columns}const theme=host.querySelector('[data-theme-mode]:checked');if(theme)state.editor.theme_mode=theme.dataset.themeMode;}

  const laterPreview=debounce(runPreview,220);
  async function runPreview(){if(!state.editor)return;sync();if(!(state.editor.reports||[]).length){state.preview=null;render();return}try{state.preview=(await request("/api/screens/preview",json("POST",state.editor))).payload;notify("")}catch(error){state.preview={error:error.message}}render();}
  async function loadTheme(){state.theme=null;if(state.editor?.id&&state.editor.theme_mode==="custom")try{state.theme=(await request(`/api/screen-themes/${encodeURIComponent(state.editor.id)}`)).theme}catch(error){notify(error.message,true)};}
  async function open(item=null){state.editor=item?structuredClone(item):blank();state.step=0;state.preview=null;state.theme=null;syncTables(state.editor);await loadTheme();render();laterPreview();}
  async function save(){sync();notify("Saving Screen…");try{const endpoint=state.editor.id?`/api/screens/${encodeURIComponent(state.editor.id)}`:"/api/screens";const data=await request(endpoint,json(state.editor.id?"PUT":"POST",state.editor));state.editor=structuredClone(data.screen);notify("Screen saved.");await load();await loadTheme();render();laterPreview()}catch(error){notify(error.message,true)}}
  async function saveTheme(){if(!state.editor?.id)return;const colors={...(state.theme?.colors||{})};host.querySelectorAll('[data-theme-color]').forEach(el=>colors[el.dataset.themeColor]=el.value);const hero=Number(host.querySelector('#screenHeroScale')?.value||100);try{state.theme=(await request(`/api/screen-themes/${encodeURIComponent(state.editor.id)}`,json("PUT",{...state.theme,colors,hero_scale:hero}))).theme;notify("Theme saved.");render()}catch(error){notify(error.message,true)}}
  async function uploadAsset(key){const input=host.querySelector(`[data-theme-asset="${CSS.escape(key)}"]`),file=input?.files?.[0];if(!file){notify("Choose an image first.",true);return}const form=new FormData();form.append("asset",file);try{state.theme=(await request(`/api/screen-themes/${encodeURIComponent(state.editor.id)}/assets/${encodeURIComponent(key)}`,{method:"POST",body:form})).theme;notify("Asset applied.");render()}catch(error){notify(error.message,true)}}

  function bind(){host.querySelectorAll('[data-action]').forEach(button=>button.addEventListener("click",async()=>{const a=button.dataset.action,id=button.dataset.id;sync();if(a==="new")return open();if(a==="edit")return open(screen(id));if(a==="close"){state.editor=null;state.preview=null;render();return}if(a==="back"){state.step=Math.max(0,state.step-1);render();laterPreview();return}if(a==="next"){state.step=Math.min(3,state.step+1);if(state.step===3)await loadTheme();render();laterPreview();return}if(a==="preview")return runPreview();if(a==="save")return save();if(a==="new-filter"){filterManager.openCreate(async created=>{state.filters=(await request("/api/filters")).filters||[];state.editor.filter_ids=[...(state.editor.filter_ids||[]),created.id].filter((v,i,a)=>a.indexOf(v)===i);render();laterPreview()});return}if(a==="open-display"){window.open(`/api/display/render?screen_id=${encodeURIComponent(id)}`,"_blank");return}if(a==="delete"){if(!confirm("Delete this Screen?"))return;try{await request(`/api/screens/${encodeURIComponent(id)}`,{method:"DELETE"});notify("Screen deleted.");await load()}catch(error){notify(error.message,true)}return}if(a==="save-theme")return saveTheme();if(a==="reset-theme"){try{state.theme=(await request(`/api/screen-themes/${encodeURIComponent(state.editor.id)}`,{method:"DELETE"})).theme;notify("Theme reset.");render()}catch(error){notify(error.message,true)}return}if(a==="upload-asset")return uploadAsset(button.dataset.asset);if(a==="reset-asset"){try{state.theme=(await request(`/api/screen-themes/${encodeURIComponent(state.editor.id)}/assets/${encodeURIComponent(button.dataset.asset)}`,{method:"DELETE"})).theme;notify("Asset removed.");render()}catch(error){notify(error.message,true)}}}));host.querySelectorAll('[data-step]').forEach(button=>button.addEventListener("click",async()=>{sync();state.step=Number(button.dataset.step);if(state.step===3)await loadTheme();render();laterPreview()}));host.querySelectorAll('input,select').forEach(el=>{if(el.dataset.themeColor!==undefined||el.id==="screenHeroScale"||el.dataset.themeAsset!==undefined)return;el.addEventListener(el.dataset.screenName!==undefined?"input":"change",async()=>{sync();if(el.dataset.themeMode!==undefined)await loadTheme();render();laterPreview()})});}

  return {load,refresh:load};
}
