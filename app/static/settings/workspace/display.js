import {esc,request,json} from "./runtime.js";

export function createDisplayWorkspace({host,notify}){
  const state={display:null};
  const screen=id=>(state.display?.screens||[]).find(item=>String(item.id)===String(id))||null;
  async function load(){state.display=await request("/api/display");render();}
  function render(){if(!state.display){host.innerHTML='<div class="empty">Loading Display…</div>';return}const d=state.display,screens=d.screens||[],rotation=new Set(d.rotation_screen_ids||[]);host.innerHTML=`<div class="card"><div class="toolbar"><div><h2>Display</h2><div class="muted small">Choose what Screen is playing now and which Screens rotate.</div></div><a class="button" href="/" target="_blank">Open Display</a></div>${screens.length?`<div class="grid" style="margin-top:12px"><div><label>Active Screen</label><select id="activeScreen">${screens.map(item=>`<option value="${esc(item.id)}" ${item.id===d.active_screen_id?"selected":""}>${esc(item.name)}</option>`).join("")}</select></div><div><label>Currently playing</label><div class="item"><strong>${esc(screen(d.current_screen_id)?.name||d.current_screen_id||"None")}</strong></div></div></div><div class="card" style="margin-top:14px"><label class="check-row"><input id="rotationEnabled" type="checkbox" ${d.rotation_enabled?"checked":""}><span><strong>Rotate Screens</strong><br><span class="muted small">Cycle through selected Screens.</span></span></label><div style="max-width:260px;margin-top:10px"><label>Seconds per Screen</label><input id="rotationSeconds" type="number" min="5" max="3600" value="${Number(d.rotation_seconds||15)}"></div><div class="check-list" style="margin-top:12px">${screens.map(item=>`<label class="check-row"><input type="checkbox" data-rotation-id="${esc(item.id)}" ${rotation.has(item.id)?"checked":""}><span>${esc(item.name)}</span></label>`).join("")}</div></div><div class="actions"><button class="button primary" data-action="save">Save Display</button><button class="button" data-action="reload">Reload</button></div>`:'<div class="empty" style="margin-top:12px">Create a Screen first.</div>'}</div>`;host.querySelectorAll('[data-action]').forEach(button=>button.addEventListener("click",()=>handle(button.dataset.action)));}
  async function handle(action){if(action==="reload")return load().catch(error=>notify(error.message,true));if(action!=="save")return;const payload={active_screen_id:host.querySelector('#activeScreen')?.value||"",rotation_enabled:!!host.querySelector('#rotationEnabled')?.checked,rotation_seconds:Number(host.querySelector('#rotationSeconds')?.value||15),rotation_screen_ids:Array.from(host.querySelectorAll('[data-rotation-id]:checked')).map(input=>input.dataset.rotationId)};notify("Saving Display…");try{state.display={...(await request("/api/display",json("PUT",payload))),screens:(await request("/api/screens")).screens||[]};notify("Display saved.");render()}catch(error){notify(error.message,true)}}
  return {load,refresh:load};
}
